
// DELETED: constants removed for tablet-only build.
// kept as a tiny placeholder to avoid breaking legacy imports. Remove this
// file only after confirming no residual references remain.
export const DELETED_CONSTANTS = true;